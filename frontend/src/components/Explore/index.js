import React from "react";
import styles from "./style.scss";
import classNames from "classnames/bind";

const cx = classNames.bind(styles);

const Explore = () => <div className={cx("container")}>Explore</div>;

export default Explore;
